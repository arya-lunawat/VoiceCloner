# All free / open-source. No paid API keys required.
# Updated for Python 3.13 compatibility (pre-built wheels on Windows)
# Pinned to exact versions for reproducibility.
# Generated from working venv — regenerate with `pip freeze > requirements.txt`
# when upgrading dependencies.

fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4

# Audio processing
librosa==0.11.0
soundfile==0.14.0
pydub==0.25.1
numpy==1.26.4
scipy==1.17.1

# Voice activity detection (lightweight, free)
webrtcvad==2.0.10

# TTS / voice cloning engine (Coqui XTTS v2)
# torch and torchaudio are NOT pinned here — TTS==0.22.0 requires
# torch>=2.1 and torchaudio, and pip will resolve a compatible pair.
# If you need explicit CUDA/cpu control, install torch separately first.
TTS==0.22.0

# Simple watermarking / metadata tagging
mutagen==1.48.1

